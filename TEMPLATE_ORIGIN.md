# Template origin

Started from the canonical external Sero Kanban plugin template at commit `4d489b1`, then replaced with the independent `@sero-ai/plugin-openai-extender` implementation. No template runtime remains.
